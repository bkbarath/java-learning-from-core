Methods:

**_Method syntax_**:
    
    return_data_type function_name (// if have parameters) {
        // body of the function
        return value;
        the return value should be same data_type as return_data_type;
    }